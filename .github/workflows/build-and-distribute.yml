name: Build and Distribute

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

on:
  workflow_dispatch:
    
jobs:
  build-win:

    runs-on: windows-latest

    strategy:
      matrix:
        node-version: [18.x]
    steps:
    
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm install
    - run: npm run build
    - run: Remove-Item -path .\dist\win-unpacked\ -recurse  # This will make sure that Build Artifact doesn't take a butt load of time to zip up the dist folder I need
    - name: SFTP uploader
      uses: wangyucode/sftp-upload-action@v1.1
      with:
        host: ${{ secrets.UPDATE_SFTP_IP }}
        port: 22
        removeExtraFiles: false
        username: ${{ secrets.UPDATE_SFTP_USERNAME }}
        password: ${{ secrets.UPDATE_SFTP_PASSWORD }}
        localDir: ./dist/
        remoteDir: /usr/share/nginx/releases/applications/discord-unpsy/latest/.prepare/windows/

  build-mac:

    runs-on: macOS-11

    strategy:
      matrix:
        node-version: [18.x]
    steps:
    
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
    - run: npm install
    - run: npm run build
    - run: sudo rm -R ./dist/mac/
    - name: SFTP uploader
      uses: wangyucode/sftp-upload-action@v1.1
      with:
        host: ${{ secrets.UPDATE_SFTP_IP }}
        port: 22
        removeExtraFiles: false
        username: ${{ secrets.UPDATE_SFTP_USERNAME }}
        password: ${{ secrets.UPDATE_SFTP_PASSWORD }}
        localDir: ./dist/
        remoteDir: /usr/share/nginx/releases/applications/discord-unpsy/latest/.prepare/macos/

  build-linux:

    runs-on: ubuntu-22.04

    strategy:
      matrix:
        node-version: [18.x]
    steps:
    
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm install
    - run: npm run build
    - run: sudo rm -R ./dist/linux-unpacked/
    - name: SFTP uploader
      uses: wangyucode/sftp-upload-action@v1.1
      with:
        host: ${{ secrets.UPDATE_SFTP_IP }}
        port: 22
        removeExtraFiles: false
        username: ${{ secrets.UPDATE_SFTP_USERNAME }}
        password: ${{ secrets.UPDATE_SFTP_PASSWORD }}
        localDir: ./dist/
        remoteDir: /usr/share/nginx/releases/applications/discord-unpsy/latest/.prepare/linux/

  release-update:
    runs-on: discord-unspy-update
    
    strategy:
      matrix:
        node-version: [18.x]
    steps:
    
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
    - run: sleep 320
    - run: cp /usr/share/nginx/releases/applications/discord-unpsy/latest/.prepare/windows/* /usr/share/nginx/releases/applications/discord-unpsy/latest/
    - run: cp /usr/share/nginx/releases/applications/discord-unpsy/latest/.prepare/macos/* /usr/share/nginx/releases/applications/discord-unpsy/latest/
    - run: cp /usr/share/nginx/releases/applications/discord-unpsy/latest/.prepare/linux/* /usr/share/nginx/releases/applications/discord-unpsy/latest/